export default class MaxServiceCost {
    constructor(phoneNumber, maxCall, maxSms, maxInternet) {
        this.phoneNumber = phoneNumber
        this.maxCall = maxCall
        this.maxSms = maxSms
        this.maxInternet = maxInternet

    }
}